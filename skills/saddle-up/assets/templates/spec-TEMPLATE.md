# Spec: {{FEATURE_OR_PROJECT_NAME}}

<!-- How to produce this: have the agent interview you — "ask me one question
     at a time until you can write a thorough spec" — then review THIS
     document instead of reviewing code later. Freeze it before
     implementation; changes during implementation get appended under
     Amendments, not silently edited.
     This file stays FUNCTIONAL (what/why/done-when). Technical decisions
     (architecture, data model, sequencing) belong under Milestones/design
     notes — or a sibling design.md for large features. Keep the whole spec
     ≤ ~2 pages; link docs/kb/ cards instead of restating repo facts. -->

## Goal
<!-- What outcome exists when this is done, in user/product terms. -->

## Non-goals
<!-- Explicitly out of scope. This list is what prevents gold-plating. -->

## Constraints
<!-- Compatibility, performance budgets, security boundaries,
     "we don't do it that way here" rules. -->

## Done when (machine-checkable)
<!-- Prefer EARS form — "WHEN <condition> THE SYSTEM SHALL <behavior>" —
     each criterion compiles almost verbatim into a feature_list.json step.
     Every criterion is a command to run or a flow to execute with an
     unambiguous pass/fail. -->
1. WHEN … THE SYSTEM SHALL …
2.
3.

## Open questions
<!-- NEVER guess. Every ambiguity becomes a marker:
     [NEEDS CLARIFICATION: specific question]
     Attended: these are the next interview questions. Unattended: each
     resolved marker becomes a numbered assumption in docs/saddle/design.md.
     The adversarial spec review fails the spec while any marker remains. -->

## Validation commands
<!-- The exact commands that prove the criteria above, e.g.:
     ./scripts/verify && <the feature's own end-to-end check>. -->

## Milestones / design notes
<!-- Only for multi-session work: each milestone has its own acceptance
     criteria + validation commands; stop-and-fix applies per milestone.
     Large features: move technical design to a sibling design.md
     (architecture, data model, sequence, testing strategy). -->

## Amendments
<!-- Dated changes to this spec, appended during implementation. -->
